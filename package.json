{
  "name": "employee-manager",
  "version": "1.0.0",
  "description": "serverless project for learn about it",
  "main": "handler.js",
  "scripts": {
    "deploy:dev": "sls deploy --stage dev --region us-east-1",
    "remove:dev": "sls remove --stage dev --region us-east-1",
    "offline": "sls offline --stage dev --region us-east-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-offline": "^12.0.4"
  },
  "dependencies": {
    "http-status-codes": "^2.2.0"
  }
}
